Expand to full Wheel Decide link generator

- Add sections for users to add wheel duration, title. diameter and removove segment on landing (done)
- HTTP values: width(wheel diameter, 100-3000), t(wheel title), time(duration), remove(if 1, remove segment upon landing)
- Add conditions to ignore colours and weight if they are all the same value for URLs (done)
- Add menu bar and credits
- Add button to copy link and values (done)

JSON wheel structure

{
	title: string,
	size: number,
	seconds to spin: number,
	remove selected value: boolean,
	segments:[
		value: string,
		background colour: string,
		text colour: string
		weight: number
	]
}